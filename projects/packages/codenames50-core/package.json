{
  "name": "codenames50-core",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "author": "psousa50",
  "license": "ISC",
  "keywords": [],
  "scripts": {
    "prebuild": "yarn run clean",
    "postbuild": "yarn test",
    "build": "tsc -d -p tsconfig.dist.json",
    "tsn": "ts-node",
    "tsc": "tsc --project tsconfig.json",
    "clean": "rm -rf lib",
    "test": "jest --testPathPattern=test/.+\\.spec\\.ts",
    "test:watch": "yarn test -- --watch",
    "prettier": "prettier --no-semi --tab-width 2 --print-width 120 --trailing-comma all --parser typescript --list-different \"{src,test}/**/*.ts\"",
    "prettier-fix": "prettier --no-semi --tab-width 2 --print-width 120 --trailing-comma all --parser typescript --write \"{src,test}/**/*.ts\""
  },
  "dependencies": {
    "ramda": "^0.27.0",
    "@psousa50/shared": "^0.1.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/ramda": "^0.27.6",
    "jest": "24.9.0",
    "ts-jest": "^24.3.0",
    "ts-node": "^8.10.1",
    "typescript": "^3.7.4"
  }
}
