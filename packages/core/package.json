{
  "name": "@codenames50/core",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "author": "psousa50",
  "license": "ISC",
  "keywords": [],
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run build:ts",
    "build:ts": "tsc -b .",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "jest",
    "test:watch": "yarn test --watch",
    "coverage": "jest --coverage"
  },
  "dependencies": {
    "@psousa50/shared": "^0.1.0",
    "ramda": "^0.27.0"
  },
  "devDependencies": {
    "@codenames50/tools": "0.0.1"
  }
}
