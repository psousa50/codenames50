{
  "name": "codenames-core",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "author": "psousa50",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/psousa50/codenames-core.git"
  },
  "keywords": [],
  "bugs": {
    "url": "https://github.com/psousa50/codenames-core/issues"
  },
  "homepage": "https://github.com/psousa50/codenames-core#readme",
  "scripts": {
    "prebuild": "npm run clean && npm run lint",
    "postbuild": "npm test",
    "build": "npm run tsc",
    "tsn": "ts-node",
    "tsc": "tsc --project tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tslint --project tsconfig.json",
    "test": "jest --testPathPattern=test/.+\\.spec\\.ts",
    "test:watch": "npm test -- --watch",
    "prettier": "prettier --no-semi --tab-width 2 --print-width 120 --trailing-comma all --parser typescript --list-different \"{src,test}/**/*.ts\"",
    "prettier-fix": "prettier --no-semi --tab-width 2 --print-width 120 --trailing-comma all --parser typescript --write \"{src,test}/**/*.ts\""
  },
  "dependencies": {
    "ramda": "^0.27.0"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "@types/ramda": "^0.27.6",
    "jest": "^26.0.1",
    "ts-jest": "^26.0.0",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.3"
  }
}
